"""Conversation-domain services (Phase 2).

Lifecycle + history orchestration over the conversation repositories. Kept
separate from the memory-domain services: conversations are the raw material,
memory is the distilled product (see PHASE2_PLAN.md §11).
"""

"""Goals domain services (M5 Goals System).

The first-class home for user-facing goals and their milestones. Routers and
the Agent Framework's context builder consume these services; the dependency
direction is one-way (agents → goals), never the reverse.
"""

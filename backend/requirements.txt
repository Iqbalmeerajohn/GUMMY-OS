# Runtime dependencies (pip fallback — `uv`/pyproject.toml is the source of truth).
# Install:  pip install -r requirements.txt
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
greenlet>=3.1
alembic>=1.13
pgvector>=0.3
anthropic>=0.40
httpx>=0.27
pyjwt[crypto]>=2.8
# Default embedding provider is OpenAI (hosted API via httpx — no torch/CUDA).
# The local Hugging Face provider is OPTIONAL and heavy (pulls torch). Install it
# only when running with EMBEDDING_PROVIDER=hf:
#   pip install "sentence-transformers>=3.0"

# Runtime dependencies (pip fallback — `uv`/pyproject.toml is the source of truth).
# Install:  pip install -r requirements.txt
fastapi>=0.115
uvicorn[standard]>=0.32
# Multipart form parsing — required by FastAPI for file uploads (M6 Files).
python-multipart>=0.0.9
# File text extraction (M6 Files): PDF + DOCX + XLSX. Pure-Python, lightweight;
# imported lazily so they are only needed when that format is uploaded.
pypdf>=5.0
python-docx>=1.1
openpyxl>=3.1
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
greenlet>=3.1
alembic>=1.13
pgvector>=0.3
anthropic>=0.40
httpx>=0.27
pyjwt[crypto]>=2.8
# Error monitoring + performance tracing (FastAPI/Starlette/asyncio integrations).
sentry-sdk[fastapi]>=2.0
# LLM/agent observability — token usage, latency, cost, retrieval/agent traces.
# No-op unless LANGFUSE_PUBLIC_KEY + LANGFUSE_SECRET_KEY are set.
langfuse>=4.0,<5.0
# Default embedding provider is OpenAI (hosted API via httpx — no torch/CUDA).
# The local Hugging Face provider is OPTIONAL and heavy (pulls torch). Install it
# only when running with EMBEDDING_PROVIDER=hf:
#   pip install "sentence-transformers>=3.0"
